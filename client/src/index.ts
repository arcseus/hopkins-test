/**
 * Main barrel export for the entire client library.
 * 
 * This is the Composition Root for the client application, providing
 * a single entry point for all public APIs. It follows the Dependency
 * Inversion Principle by exposing stable abstractions rather than
 * concrete implementations.
 */

// Export all types
export * from './types';

// Export all services
export * from './services';

// Export all utilities
export * from './utils';
